# ──────────────────────────────────────────────────────────────
# CollabBoard Environment Variables
# ──────────────────────────────────────────────────────────────
#
# TWO-FILE CONVENTION:
#   .env        — Production credentials (used by Vercel as fallback)
#   .env.local  — Local dev credentials (overrides .env when present)
#
# Vite loads .env.local first, then .env. So when .env.local exists,
# local dev hits the local Supabase and production stays untouched.
#
# LOCAL DEVELOPMENT (with local Supabase via Docker/OrbStack):
#   npx supabase start        → boots local Postgres, Auth, Realtime
#   npx supabase status -o json → prints the keys below
#   npx supabase db reset     → wipe & re-run all migrations
#   npx supabase stop         → shut down containers
#   Studio UI: http://127.0.0.1:54323
# ──────────────────────────────────────────────────────────────

# Supabase (client-side — exposed to browser via VITE_ prefix)
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=your-local-anon-key

# Supabase (server-side — Vercel serverless functions only)
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=your-local-service-role-key

# OpenAI (server-side — AI agent)
OPENAI_API_KEY=sk-your-openai-key

# LangSmith (observability — optional)
# Create two projects at smith.langchain.com: one for dev, one for prod.
# Set LANGSMITH_PROJECT to the appropriate project name per environment.
# Set LANGSMITH_TRACING=false to disable tracing entirely.
LANGSMITH_API_KEY=lsv2_pt_your-langsmith-key
LANGSMITH_PROJECT=collabboard-dev
LANGSMITH_TRACING=true
